name: mymoneyclone
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3
  iconsax_flutter: ^1.0.1
  path_provider: ^2.1.5
  sqflite: ^2.4.2
  flutter_sticky_header: ^0.8.0
  flex_menu_button: ^1.0.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
